# AI Provider SDKs (install at least one)
google-generativeai>=0.3.0  # For Gemini (FREE tier)
anthropic>=0.39.0           # For Claude (paid, $5 free credits)

# Core dependencies
mcp>=1.0.0
requests>=2.31.0
python-dotenv>=1.0.0
